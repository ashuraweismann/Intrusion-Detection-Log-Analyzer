Shadow IDS GUI Portable Build

Run:
  ShadowIDS_GUI.exe

This folder already includes the Qt runtime DLLs and plugins needed by the app.
Do not move ShadowIDS_GUI.exe away from the DLL files and folders beside it.

To share the app, zip this entire folder and send it as one package.

Included sample file:
  logs.txt
